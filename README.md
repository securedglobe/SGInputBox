# SGInputBox

Code Project InputBox helper (`SG_InputBox`) with a TestInputBox host.

Open `SG_TestInputBox.sln` in Visual Studio.

## Unit tests

`Tests/InputBoxTests` checks aspect ratio, input-box size, button width, and window class. Build Release|x64.
